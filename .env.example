# SkillGuard server environment variables
# Copy to .env and fill in values as needed.

# Logging level (default: info)
RUST_LOG=info

# API key for bearer token authentication on /api/v1/* endpoints.
# If unset, auth is disabled (backward compatible).
# SKILLGUARD_API_KEY=your-secret-key-here

# Wallet address to receive x402 USDC payments on Base.
# If unset, x402 payment is disabled (API key only).
# SKILLGUARD_PAY_TO=0xYourBaseWalletAddress

# x402 facilitator URL (defaults to free OpenFacilitator).
# SKILLGUARD_FACILITATOR_URL=https://pay.openfacilitator.io

# External base URL for x402 resource URLs behind a reverse proxy / TLS terminator.
# SKILLGUARD_EXTERNAL_URL=https://skillguard.onrender.com

# Set to "1" or "true" to disable the ZKML prover (evaluate endpoints will return errors).
# SKILLGUARD_SKIP_PROVER=0

# Price per classification in USDC micro-units (6 decimals). Default: 1000 ($0.001).
# SKILLGUARD_PRICE_USDC_MICRO=1000
